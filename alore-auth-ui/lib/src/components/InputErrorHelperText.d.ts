import React from 'react';
interface Props {
    id?: string;
    message: string;
}
declare const InputErrorHelperText: ({ id, message }: Props) => React.JSX.Element;
export default InputErrorHelperText;
